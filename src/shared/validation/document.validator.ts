/**
 * Valida CPF ou CNPJ (apenas dígitos, sem formatação).
 * Implementação placeholder — a lógica final deve validar dígitos verificadores.
 */
export function isValidDocument(_document: string): boolean {
  throw new Error('Not implemented');
}
